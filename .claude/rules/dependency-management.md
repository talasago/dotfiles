---
paths:
  - "package.json"
  - "pnpm-lock.yaml"
  - "package-lock.json"
  - "yarn.lock"
  - "Gemfile"
  - "Gemfile.lock"
---

# 依存関係の管理

- 直接依存するパッケージのバージョンは、可能な限り完全固定する。
- 間接依存はロックファイルで再現可能な状態を維持する。
- 依存関係を追加・更新した場合は、利用可能なセキュリティ監査コマンドを実行する。
- 監査で問題が検出された場合は、脆弱性の影響・修正可能性・対応方針を確認する。
- パッケージマネージャー固有のコマンドや手順は、各ルールファイルに従う。
- アプリ本体の通常のビルド・実行に含まれない運用・開発補助スクリプト（定期実行するレポート取得スクリプト等）を追加する際は、その依存関係をアプリ本体のマニフェストに混ぜてよいか、独立したマニフェスト（別ディレクトリ、モノレポのworkspace外）に分離すべきかを実装前に確認する。
